# PRD｜短剧分支共创平台「StoryTree」v4

> 黑客松项目 · Video Generation (Co-host by PixVerse) · 用 TRAE 搭建
> 版本 v4.0（含完整经济模型 + 线框级 UI）｜ 面向：开发可直接照建 + 答辩对齐
> 评分锚点：**视频生成 40% ｜ App 完整度 30% ｜ TRAE 有效性 30%**

---

## 0. 一句话定义

一个会自我进化的群体共创短剧宇宙：平台放出剧情起点，用户在任意节点用 PixVerse 生成自己的分支续写，剧情长成一棵树；读者为喜欢的分支点赞、付费解锁，低赞分支被自然 drop，整棵树由集体审美与真实付费修剪进化。创作者付 token 生成、读者付费解锁回馈创作者，形成双边经济飞轮。

---

## 1. 评分对齐与开发纪律

| 评分项 | 权重 | 落地动作 | 验收标准 |
|---|---|---|---|
| 视频生成 | 40% | 母片 + 多分支均为完整 PixVerse 视频，预置 6-8 条构成有形状的树 | 每条 PixVerse 生成、风格统一、母片≥30s、分支接得上前情 |
| App 完整度 | 30% | 「浏览树 → 看分支 → 生成新分支 → like → 付费 → drop」闭环现场可点通 | 评委能浏览树、点赞、走一次生成、看到 drop |
| TRAE 有效性 | 30% | 全程 TRAE 搭建并保留可展示过程 | 能演示 TRAE 生成树形组件/调试/迭代 |

**铁律：**
1. 视频提前生成存为静态资源；现场"生成新分支"演示**一次**成功路径作亮点，其余用预置。
2. 付费、token、分成全部是**展示性 UI（mock，不真实扣费）**，不进 P0 开发，只服务答辩。
3. 所有分支视频用统一角色圣经 + 同套 PixVerse 风格参数，缓解一致性。
4. drop 的时间逻辑（30天等）**在 pre 里用生命周期图讲**，不做现场动态时间流逝。
5. 本版**不做**相似分支合并，专注生成+like+drop+微调衍生。

---

## 2. 完整产品模型（终态，所有决策已收敛）

### 2.1 内容层
- 母片（树根）免费看。
- 每个分支：**梗概免费看 → 看完可 like → 完整视频前 30 天免费，30 天后付费解锁**。
- 新用户可**复制父分支的提示词微调**生成新分支，标注「衍生自 @某人」，形成创作血缘链。

### 2.2 经济层（双边飞轮）
```
创作者 付 token 生成分支 ──→ 平台赚 token 钱
读者   付费解锁完整视频 ──→ 钱给创作者（平台抽成）
                              ↓
            创作者赚钱 → 更有动力创作 → 消耗更多 token
            ↑________________________________________|
```

### 2.3 进化层（drop）
- **生存线**：过 7 天保护期后，分支 like 率（梗概兴趣度 + 免费期完整片口碑）太低 → 标记 endangered → 持续低迷 → drop。
- **晋级线**：付费期内付费转化率高 → 晋级「主线」获更多曝光与续写机会。
- **drop ≠ 删除**：进回收站，可被重新发现并大量 like/付费而复活，保留创作者署名。
- **时间逻辑**：前 30 天免费期看完整片攒 like（口碑可信），30 天后转付费、看付费转化。**这套生命周期在 pre 里用图讲，demo 用预置不同年龄分支静态呈现。**

---

## 3. 设计语言（UI 总基调）

> 目标：让评委一眼记住。剧情树是核心视觉，要有「生命感」——节点会发光、枯萎、掉落。

- **主题方向**：暗色「夜间剧场 / 星图」基调——深色背景上，剧情树像一条**从左向右流淌的发光星河**，母片是源头，每个分支是一颗星，越受欢迎越亮，濒危的逐渐黯淡。横向的流向同时承载「时间推进」「树生长」和「优胜劣汰」，记忆点强。
- **配色**：深底（近黑的深蓝/紫调）+ 高饱和强调色（健康=暖金/青绿光晕，濒危=暗橙渐灰，drop=冷灰）。避免白底紫渐变这种 AI 套路色。
- **字体**：标题用有个性的展示字体（如衬线感强的戏剧化字体，呼应「剧」），正文用一款干净的无衬线体；中文标题可用思源宋体加重，正文思源黑体。
- **动效（高光时刻，集中投入）**：
  - 节点 like 时：星点亮一下 + 光晕扩散。
  - 新分支生成并加入：从父节点「生长」出一条连线 + 新星点亮的入场动画。
  - drop 时：星点黯淡、缩小、脱离主树掉落。
  - 这三个动效是 demo 的视觉爆点，值得用 Motion 库精心做。
- **背景**：深色 + 细微噪点/星尘纹理 + 树的连线有微弱流光，营造「活的宇宙」氛围。

---

## 4. 页面与路由

```
/                  首页（剧情宇宙入口 + 热门分支）
/tree/:storyId     ★剧情树主视图（核心页面）
/node/:nodeId      单分支详情（看视频 + like + 付费 + 续写入口）
/create/:parentId  生成新分支（弹窗形式，叠加在 tree 上）
/recycle           回收站（被 drop 的分支，P1）
/profile           我的创作（P1）
```

---

## 5. 线框级 UI 设计（开发照此搭）

### 5.1 首页 `/`

```
┌─────────────────────────────────────────────┐
│  StoryTree            [我的创作] [回收站]      │ ← 顶栏，logo + 入口
├─────────────────────────────────────────────┤
│                                               │
│   〔母片大封面卡〕                             │
│   《豪门弃妇的逆袭》                            │
│   一个开放结局的起点 · 已有 23 个分支生长中     │
│   [进入剧情树 →]                               │
│                                               │
│   ── 热门分支（按 like 率）──                  │
│   [缩略图][缩略图][缩略图][缩略图]  横向滚动     │
│    🔥134   🔥98    🔥87    🔥56              │
└─────────────────────────────────────────────┘
```

### 5.2 剧情树主视图 `/tree/:storyId` ★核心页面

整页是一棵**横向、根在最左、向右生长**的剧情树（星图风格），可缩放、可横向拖拽。从左到右 = 故事时间线推进，越往右剧情越深。这是开发资源的重点。

```
┌──────────────────────────────────────────────────────────────┐
│  ← 返回   《豪门弃妇的逆袭》剧情树            [筛选: 全部▾]      │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│              ┌─◉ 主线分支A ──◉ 二层A1                          │
│              │              └─◉ 二层A2                         │
│   ◉ 母片(根)─┼─◉ 普通分支B ──◉ 二层B1   [＋]←悬停右侧浮现       │
│              │                                                 │
│              └─◔ 濒危分支C(黯淡)                               │
│                                                                │
│   ←────────── 横向拖拽 / 滚动 ──────────→  (向右无限生长)       │
│                                                                │
│  ┌─图例──────────────┐  ┌─minimap─┐   [- 缩放 +] [回到根(左)] │
│  │🟢健康 🟡濒危 ⚫drop│  │ ▢▢▢ □   │                          │
│  └───────────────────┘  └─────────┘                          │
└──────────────────────────────────────────────────────────────┘
```

**布局规则：**
- **根（母片）固定在最左**，子分支向右展开，父→子连线从节点右侧出、左侧入。
- 主滚动/拖拽方向为**横向**；树向右无限延伸。
- 右下角 **minimap**（react-flow 自带）显示当前在树的哪一段，深层不迷路。
- 「回到根」= 跳回最左端母片。

**节点（BranchNode）交互——你描述的核心交互：**
- 每个节点显示：缩略图 + 标题(截断) + 🔥like数 + 健康度光晕（颜色按 status）。
- **鼠标悬停节点** → 节点放大微动 + **右侧浮现一个「＋」按钮**（右=未来=续写，方向天然契合）+ 弹出小信息卡（作者、like率、免费/付费状态）。
- **点节点本体** → 进入 `/node/:nodeId` 详情页。
- **点「＋」按钮** → 弹出「生成新分支」弹窗（见 5.4），新分支挂在该节点右侧，parentId = 该节点。
- 濒危节点视觉上黯淡/有轻微「枯萎」抖动；drop 的节点灰化并脱离主树（或移入回收站）。
- 新分支加入时的「生长动画」= 从父节点**向右**延伸出连线 + 新星点亮。

### 5.3 单分支详情 `/node/:nodeId`

```
┌─────────────────────────────────────────────┐
│  ← 返回剧情树                                  │
│                                               │
│   〔梗概区 - 永远免费可见〕                     │
│   「她当众撕毁了那张支票，全场哗然……」          │
│                                               │
│   ┌─────────────────────────┐                │
│   │                         │                │
│   │   视频播放区             │ ← 免费期:直接播放 │
│   │   [▶]                   │   付费期:见下方遮罩 │
│   │                         │                │
│   └─────────────────────────┘                │
│   〔付费期遮罩〕                               │
│   🔒 30天免费期已过 · 解锁完整视频              │
│   [付费解锁 ¥1.9 →]  ← mock,点了展示分成提示   │
│                                               │
│   作者 @匿名#42 · 衍生自 @某人 · day 12(免费中) │
│   [❤ Like 134]   [✎ 在此续写]                 │
│                                               │
│   ── 后续分支 ──                               │
│   [子分支缩略图] [子分支缩略图] [＋新分支]      │
└─────────────────────────────────────────────┘
```

- **梗概区**：永远免费，是 like 的依据入口。
- **视频区**：免费期(≤30天)直接可播；付费期叠加遮罩 + 解锁按钮。
- **付费解锁**（mock）：点击 → 弹「¥1.9 已支付，创作者获得 ¥1.5，平台 ¥0.4」展示性提示 → 解除遮罩播放。**不真实扣费**。
- **Like 按钮**：看完梗概即可点，触发节点点亮动效 + like 数实时更新。
- **在此续写**：= 5.4 生成弹窗，parentId = 当前节点。
- **血缘标注**：「衍生自 @某人」点击可跳父节点。

### 5.4 ★生成新分支弹窗 `/create/:parentId`（你重点描述的，逐字段画出）

从节点的「＋」或详情页「在此续写」触发，弹窗叠加在当前页之上。

```
┌──────────────────────────────────────────────┐
│  生成新分支                              [✕]    │
├──────────────────────────────────────────────┤
│  接续自：「她当众撕毁了那张支票……」（父节点梗概）│ ← 让用户知道接什么
│                                                │
│  ① 提示词                                       │
│  ┌────────────────────────────────────────┐  │
│  │ [已复制父分支提示词，可直接修改]          │  │ ← 复制父prompt预填,微调衍生
│  │ 镜头：女主转身冷笑，豪门众人愕然…          │  │
│  │                                          │  │
│  └────────────────────────────────────────┘  │
│  （含隐藏的「角色圣经」前缀，保证人物一致，不可删）│ ← 一致性保障
│                                                │
│  ② 参考画面（可选）                             │
│  [上传参考图] 或 [使用父节点末帧 ✓]             │ ← 末帧衔接,接得上
│                                                │
│  ③ 风格 / 时长                                  │
│  风格：[与父级一致 ▾]   时长：[15s ▾]           │
│                                                │
├──────────────────────────────────────────────┤
│           [🔀 Shuffle 换一版]                   │ ← 不喜欢可重抽
│                                                │
│  〔生成结果预览区〕                             │
│  ┌──────────────┐                             │
│  │ [▶] 预览视频  │  生成中… / 预览              │
│  └──────────────┘                             │
│                                                │
│        [生成 (5 token)]      [使用]            │ ← 右下角:生成耗token + 采用
└──────────────────────────────────────────────┘
```

**弹窗交互流程（状态机）：**

```
打开弹窗（prompt 已预填父分支内容，可改）
   ↓ 点【生成 (5 token)】
生成中（loading；demo 用预置结果秒回 / 真生成一次作亮点）
   ↓
出现【预览视频】
   ├─ 不满意 → 点【🔀 Shuffle】→ 重新生成（再扣 5 token，展示性）
   └─ 满意 → 点【使用】
        ↓
   弹出二次确认：「确定将这个分支加入剧情树吗？」[取消] [确定加入]
        ↓ 确定
   新节点以「生长动画」加入树，挂在 parentId 下，进入 7 天保护期
   标注作者 = 当前用户，「衍生自」= 父节点作者
```

**字段说明（开发对照）：**
| 元素 | 行为 | 备注 |
|---|---|---|
| 提示词框 | 预填父分支 prompt，可编辑 | 实现「复制微调衍生」 |
| 角色圣经前缀 | 隐藏/锁定，强制带上 | 保证跨分支人物一致 |
| 参考画面 | 上传图 或 用父节点末帧 | 末帧衔接，画面接得上 |
| 风格/时长 | 默认继承父级 | 维持统一风格 |
| 生成 (5 token) | 触发生成，展示扣 5 token | mock，不真实扣费 |
| Shuffle | 重新生成一版 | 体现「不喜欢可重抽」 |
| 使用 | 采用当前预览 → 二次确认 | 防误加 |
| 二次确认弹窗 | 「确定加入剧情树？」 | 你要求的确认步骤 |

> token 计数（如「你的余额 95 token」）放弹窗顶角作展示性 UI，让评委一眼看到商业模式，成本极低。

---

## 6. 核心数据结构

### 6.1 分支节点

```json
{
  "nodeId": "node_012",
  "storyId": "story_001",
  "parentId": "node_003",
  "title": "她当众撕毁了那张支票",
  "synopsis": "她当众撕毁了那张支票，全场哗然……",
  "prompt": "镜头：女主转身冷笑…（含角色圣经前缀）",
  "authorId": "user_xx",
  "authorName": "匿名创作者#42",
  "derivedFrom": "user_yy",
  "videoUrl": "/assets/node_012.mp4",
  "duration": 22,
  "createdAt": 1717000000000,
  "ageDays": 12,
  "isFreeWindow": true,
  "unlockPrice": 1.9,
  "likes": 134,
  "impressions": 980,
  "paidUnlocks": 21,
  "isProtected": true,
  "status": "alive | endangered | dropped"
}
```

### 6.2 行为事件
```json
{ "type": "view | like | unlike | unlock_paid | generate | publish_branch",
  "nodeId": "...", "sessionId": "...", "timestamp": 0 }
```

### 6.3 预置 mock 树（demo 底子）
- 1 根（母片）+ 2-3 个一层分支（含 1 个高like主线、1 个濒危待drop）+ 2-3 个二层分支。
- 刻意安排不同 `ageDays`：有的 day 12（免费中）、有的 day 35（付费中），让评委看到两种状态。
- 预置 ~300 条 like/view 事件让健康度有数据底子。

---

## 7. 功能模块清单（含验收）

### P0 — 必做
| 模块 | 验收标准 |
|---|---|
| 剧情树可视化（StoryTreeGraph） | 树渲染、缩放拖拽、节点健康度颜色、悬停浮现＋号 |
| 节点详情 + 视频播放 | 梗概免费、免费期可播、付费期遮罩 |
| Like 交互 | 点赞即时更新数字+健康度+点亮动效 |
| drop / 健康度 | 按 like 率算 status，濒危/drop 视觉表达 |
| 生成分支弹窗 | 5.4 全流程：预填prompt→生成→shuffle→使用→二次确认→加入树 |
| mock 数据 | 预置树 + 事件，健康度有底子 |

### P1 — 时间够再做
- 微调生成的「真生成」一次成功路径（亮点）
- 付费解锁 mock 弹窗（分成展示）
- 回收站 / 我的创作页

### P2 — 不做
- 真实支付/token计费/分成结算、相似分支合并、注册登录、后端DB、动态时间流逝

---

## 8. 视频一致性方案（最大技术风险）

1. **角色圣经**：母片定义主角外貌/服装/场景，作为锁定的 prompt 前缀，所有分支强制携带。
2. **统一风格参数**：所有视频同套 PixVerse 风格。
3. **末帧衔接**：生成弹窗默认勾选「使用父节点末帧」作参考首帧。
4. **诚实兜底**：demo 用已验证接得上的预置视频；答辩主动说明这是核心挑战及解法。

---

## 9. PixVerse 视频生成清单（吃满 40%）

| 编号 | 内容 | 时长 | 角色 |
|---|---|---|---|
| V0 | 根母片（强钩子+开放结尾） | ≥30s | 树根 |
| V1 | 一层分支A（设计成高like主线） | 18-22s | 主线 |
| V2 | 一层分支B（设计成濒危） | 18-22s | 待drop |
| V3 | 一层分支C | 18-22s | 普通 |
| V4-V5 | 二层分支（挂V1后） | 各15-20s | 体现生长 |
| V6 | 现场真生成用的预置结果 | 15-20s | demo亮点 |
| V7 | 备用 | 15-20s | 时间紧可砍 |

纪律：全部带角色圣经前缀；V0 开放结尾；V1 明显比 V2 精彩（让主线/drop 在内容上说得通）；记录所有 prompt 供答辩展示一致性。

---

## 10. TRAE 使用与展示（占 30%）

- 用 TRAE 生成核心组件，重点是 **StoryTreeGraph 树可视化** 和 **生成弹窗**（复杂 UI，正好展示 TRAE 能力）。
- 用 TRAE 调试一处真实 bug（如 like 后健康度不刷新），留前后对比。
- 用 TRAE 跑一个增量需求（如「给节点加枯萎动画」）从描述到落地。
- 答辩：录屏展示 TRAE 工作流 + 一句量化提效。

---

## 11. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 框架 | React | TRAE 友好 |
| 树可视化 | react-flow | 节点/连线/缩放开箱即用，黑客松首选 |
| 动效 | Motion (framer-motion) | like/生长/drop 三大动效 |
| 视频 | 原生 `<video>` | 够用可控 |
| 状态 | React state + 内存数组 | 无后端 |
| mock | 预置树 JSON + 事件 | demo 底子 |
| 视频资源 | PixVerse mp4 | 现场零依赖 |
| 环境 | TRAE | 全程可展示 |

---

## 12. 里程碑（按依赖）

| 序 | 任务 | 优先级 | 依赖 |
|---|---|---|---|
| 1 | PixVerse 生成 V0-V5（含角色圣经） | 最高，先做 | 无 |
| 2 | TRAE 搭骨架 + StoryTreeGraph 树可视化 | P0 | 1 |
| 3 | 节点详情 + 视频播放 + like + 三大动效 | P0 | 2 |
| 4 | 健康度/drop 逻辑（看 like 率） | P0 | 3 |
| 5 | 生成分支弹窗全流程（5.4） | P0 | 2 |
| 6 | mock 树数据 + 事件 | P0 | 3 数据结构 |
| 7 | 全链路联调，现场零断点 | P0，留足时间 | 全部 |
| 8 | 真生成一次/付费mock/回收站 | P1 | 1,5 |
| 9 | TRAE 录屏 + 答辩材料 + 生命周期图 | 必做 | 并行 |

---

## 13. 答辩话术骨架

1. **开场**：短剧创作是少数人的特权，我们让它变成所有人共创、会自己进化的剧情宇宙。
2. **demo 主体**：进剧情树（星图）→ 悬停节点浮现＋号 → 点开一个分支看视频 → like 看它变亮 → **走一次生成弹窗：复制别人的 prompt 微调 → 生成(5token) → shuffle → 使用 → 确认加入 → 新星从父节点生长出来**（高光）。
3. **讲机制**：like 是养分（判生存）、付费是认可（判晋级）、drop 是修剪（进回收站可复活）。用生命周期图讲 30 天免费→付费的窗口逻辑。
4. **讲商业**：双边飞轮——创作者付 token 生成、读者付费解锁回馈创作者，平台两头赚；drop 不减收入反而提质（生成即收入，淘汰只提质）。
5. **呼应 40% 视频**：6-8 条 PixVerse 构成一棵树，角色圣经保一致。
6. **呼应 30% TRAE**：录屏展示用 TRAE 搭树可视化和生成弹窗。

---

## 14. 必被问 & 标准答案

| 问题 | 答案 |
|---|---|
| 不同用户的分支角色对不上？ | 角色圣经 prompt 前缀 + 末帧衔接 + 统一风格，识别到的核心挑战 |
| 新分支没人看就被 drop？ | 7天保护期 + 看 like 率非绝对数 + drop 进回收站可复活 |
| 梗概免费会不会出标题党？ | 前30天免费看完整片，like 基于真实观看，过滤标题党；付费转化再判晋级 |
| 30天逻辑 demo 看不到？ | pre 用生命周期图讲，demo 预置不同年龄分支静态呈现 |
| 商业模式？ | 双边飞轮：创作者付token生成（平台赚token）+ 读者付费解锁（回馈创作者，平台抽成） |
| 和普通UGC视频平台区别？ | 树状血缘结构 + 优胜劣汰自进化 + 可在别人创作上微调衍生 |

---

## 15. 风险与预案

| 风险 | 预案 |
|---|---|
| PixVerse 生成不及预期 | 第1步最先做；V6/V7可砍；分支缩到15s |
| 分支视频一致性差 | 角色圣经；demo用已验证接得上的预置视频 |
| 树可视化做不完 | react-flow 优先；最坏退化为纵向缩进列表 |
| 生成弹窗真生成翻车 | 真生成标P1；预置结果秒回兜底，真生成成功是bonus |
| 现场闭环断点 | 第7步留足联调；备录屏兜底 |
| 时间不够 | 保P0（树+视频+like+drop+生成弹窗+TRAE），砍P1全部 |
